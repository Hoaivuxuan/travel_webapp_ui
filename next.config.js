/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['static1.squarespace.com', 'bizweb.dktcdn.net'],
  },
};

module.exports = nextConfig;
